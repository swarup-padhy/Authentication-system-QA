# 02 - Manual Test Cases

## 1. Signup Module

| TC ID | Scenario ID | Test Steps | Expected Result | Actual Result | Status |
|-------|-------------|------------|-----------------|---------------|--------|
| TC-001 | TS-SIGNUP-01-01 | 1. Open App<br>2. Go to Signup<br>3. Enter valid Name, Email, Password<br>4. Submit | 201 Created. User saved to DB. JWT cookie set. Verification token linked. | Server returns HTTP 201. Password is bcrypt-hashed before storage, JWT cookie is set, and verification token + expiry are saved to the user document. | PASS |
| TC-002 | TS-SIGNUP-01-02 | 1. Enter Name string of 255 chars<br>2. Fill other fields<br>3. Submit | Request succeeds or fails gracefully based on DB schema limit (usually handled cleanly by Mongoose). | Schema defines `name` as `{ type: String, required: true }` with no `maxlength`. Request succeeds and name is stored in full. | PASS |
| TC-003 | TS-SIGNUP-01-03 | 1. Enter Name string of 1/2 chars<br>2. Fill other fields<br>3. Submit | Success (assuming no min-length defined in Mongoose). | Schema has no `minlength` on `name`. A 1- or 2-character name passes validation and the user is registered successfully. | PASS |
| TC-004 | TS-SIGNUP-01-04 | 1. Enter Name with Arabic/Chinese chars: "محمد" / "张三"<br>2. Submit | Successfully registers and stores correct Unicode encoding in DB. | Mongoose/MongoDB stores UTF-8 by default. No character restrictions exist in the schema; registration succeeds with Unicode names. | PASS |
| TC-005 | TS-SIGNUP-01-05 | 1. Enter Email: `very.long.email.address.with.many.characters@extremelylongdomainname.com`<br>2. Submit | Successfully registers. | No `maxlength` constraint on the `email` field. The long email passes the `!email` check and is stored successfully. | PASS |
| TC-006 | TS-SIGNUP-01-06 | 1. Execute TC-001<br>2. Check DB directly for user document | `password` field in DB is a bcrypt hash (length 60), not plaintext. | `bcryptjs.hash(password, 10)` is called before `user.save()`. The stored value is a 60-character bcrypt hash, not the original plaintext. | PASS |
| TC-007 | TS-SIGNUP-01-07 | 1. Execute TC-001<br>2. Intercept response headers | Response contains `Set-Cookie` with `token=` and `HttpOnly; SameSite=strict; max-age=...` directives. | `generateTokenAndSetCookie` sets `httpOnly: true`, `sameSite: "strict"`, and `maxAge: 604800000` (7 days). All expected cookie directives are present. | PASS |
| TC-008 | TS-SIGNUP-01-08 | 1. Execute TC-001<br>2. Inspect Browser DevTools Application > Cookies | JWT Token is present in the cookie storage. | Cookie is set with `httpOnly: true`, making it visible in DevTools > Application > Cookies but inaccessible via `document.cookie`. | PASS |
| TC-009 | TS-SIGNUP-01-09 | 1. Execute TC-001<br>2. Query MongoDB for new user | `verificationToken` has a 6-digit string and `verificationTokenExpiresAt` is set to +24hrs. | Token is generated via `Math.floor(100000 + Math.random() * 900000).toString()` (always 6 digits). Expiry is set to `Date.now() + 86400000`. Both fields are stored correctly. | PASS |
| TC-010 | TS-SIGNUP-02-01 | 1. Leave Name empty<br>2. Enter Email/Pass<br>3. Submit | Fails with 400. Error message: "All fields are required". | `!name` evaluates to `true` for an empty string. Controller throws `"All fields are required"` and returns HTTP 400. | PASS |
| TC-011 | TS-SIGNUP-02-02 | 1. Leave Email empty<br>2. Enter Name/Pass<br>3. Submit | Fails with 400. Error message: "All fields are required". | `!email` evaluates to `true`. Controller returns HTTP 400 with message `"All fields are required"`. | PASS |
| TC-012 | TS-SIGNUP-02-03 | 1. Leave Password empty<br>2. Enter Name/Email<br>3. Submit | Fails with 400. Error message: "All fields are required". | `!password` evaluates to `true`. Controller returns HTTP 400 with message `"All fields are required"`. | PASS |
| TC-013 | TS-SIGNUP-02-04 | 1. Send POST to `/api/auth/signup` with an empty JSON body `{}` | Fails with 400. Error message: "All fields are required". | All three fields destructure as `undefined`. The guard condition `!email \|\| !password \|\| !name` triggers and returns HTTP 400 `"All fields are required"`. | PASS |
| TC-014 | TS-SIGNUP-02-05 | 1. Enter Email: `testexample.com` (no @)<br>2. Submit | Backend Mongoose validation OR custom logic throws 400 Bad Request. | No email format validation exists in the controller or Mongoose schema. The value `testexample.com` passes all checks and is stored in the database as-is. | FAIL |
| TC-015 | TS-SIGNUP-02-06 | 1. Enter Email: `test@.com` or `@example.com`<br>2. Submit | Fails. Invalid email format. | No `match` regex or format validator on the `email` schema field. Malformed emails pass all checks and are stored in the database. | FAIL |
| TC-016 | TS-SIGNUP-02-07 | 1. Enter Email: `test@@example.com`<br>2. Submit | Fails. Invalid email format. | No email format validation in the controller or schema. The value passes the `!email` check and is stored in the database without rejection. | FAIL |
| TC-017 | TS-SIGNUP-02-08 | 1. Enter Email: ` test@example.com `<br>2. Submit | Email is successfully registered as `test@example.com` (trimmed) OR fails if not trimmed. | No trimming is applied in the controller or schema. The email is stored with leading and trailing whitespace as ` test@example.com `. | FAIL |
| TC-018 | TS-SIGNUP-02-09 | 1. Enter Password: `12345`<br>2. Submit | Fails with 400 if minimum length is enforced (Expected). Actual: likely passes due to BUG-004. | No password length or complexity validation exists in the controller or schema. The value `12345` is hashed and the user is registered successfully. *(BUG-004)* | FAIL |
| TC-019 | TS-SIGNUP-02-10 | 1. Enter Password: `123456789`<br>2. Submit | Fails with 400 if complexity is enforced. Actual: Passes. | No complexity enforcement exists. The numeric-only password `123456789` is hashed and the user is registered successfully. | FAIL |
| TC-020 | TS-SIGNUP-02-11 | 1. Enter Password: `password`<br>2. Submit | Fails with 400 if complexity is enforced. Actual: Passes. | No dictionary-word or complexity check exists. The common password `password` is hashed and the user is registered successfully. | FAIL |
| TC-021 | TS-SIGNUP-02-12 | 1. Submit existing Email: `olduser@test.com`<br>2. Submit again | Fails with 400. "User already exists". | `User.findOne({ email })` detects the existing user. Controller returns HTTP 400 `"User already exists"` before attempting to save. | PASS |
| TC-022 | TS-SIGNUP-02-13 | 1. Submit existing Email, but uppercase: `OLDUSER@test.com` | Should fail if case-insensitive indexing is implemented. | Schema has no `lowercase: true` option or case-insensitive collation. `findOne({ email: "OLDUSER@test.com" })` does not match the lowercase record, allowing a duplicate user to be created. | FAIL |
| TC-023 | TS-SIGNUP-03-01 | 1. Send Name payload of 10 Megabytes using Postman. | Request rejected immediately (413 Payload Too Large or Express parser throws). | `express.json()` uses its default 100kb body size limit. A 10MB payload exceeds this limit and Express returns HTTP 413 before the controller is reached. | PASS |
| TC-024 | TS-SIGNUP-03-02 | 1. Enter Name: `<script>alert('XSS')</script>`<br>2. Submit | Stores successfully but is strictly sanitized on output (or refused on input). | No input sanitization library is used. The raw script tag is stored in MongoDB as-is. Backend does not sanitize on input or output. | FAIL |
| TC-025 | TS-SIGNUP-03-03 | 1. Enter Email: `' OR 1=1 --`<br>2. Submit | Mongoose strict schema rejects it or hashes safely. 400 Bad Request. | Mongoose uses BSON parameterized queries, making this SQL-style string a literal value. For signup, it is stored as-is; for login, `findOne` finds no match and returns HTTP 400 `"Invalid credentials"`. | PASS |
| TC-026 | TS-SIGNUP-03-04 | 1. Stop `mongod` service.<br>2. Submit signup request. | Fails gracefully with 500 error, NO raw stack trace leaked. | The `catch` block returns HTTP 400 with `error.message` (e.g., `"buffering timed out after 10000ms"`), leaking an internal Mongoose error string. Status code is 400 instead of the expected 500. | FAIL |
| TC-027 | TS-SIGNUP-03-05 | 1. Input invalid Mailtrap token in `.env`.<br>2. Submit valid signup. | User created in DB but 500 Error returned to client due to email fail, OR handles safely gracefully. | `user.save()` completes successfully before the email call. When `sendVerificationEmail` throws, the `catch` block returns HTTP 400 with the raw mailtrap error message. User is saved but client receives an error response. | FAIL |

## 2. Login Module

| TC ID | Scenario ID | Test Steps | Expected Result | Actual Result | Status |
|-------|-------------|------------|-----------------|---------------|--------|
| TC-028 | TS-LOGIN-01-01 | 1. Go to Login<br>2. Fill correct Email & Pass<br>3. Submit | 200 OK. "Logged in successfully". Excludes password in response. | Server returns HTTP 200 `"Logged in successfully"`. User object is returned with `password: undefined`. | PASS |
| TC-029 | TS-LOGIN-01-02 | 1. Execute TC-028<br>2. Query DB | `lastLogin` timestamp is updated to current UTC time. | `user.lastLogin = new Date()` is set and `user.save()` is awaited post-login. The `lastLogin` field is updated in the database. | PASS |
| TC-030 | TS-LOGIN-01-03 | 1. Login user<br>2. Extract JWT from cookie<br>3. Decode payload | JWT payload contains `{ userId: "xxx" }` and is signed correctly (`verifyToken` passes later). | Token is signed as `jwt.sign({ userId }, JWT_SECRET, { expiresIn: "7d" })`. Decoded payload contains only `userId`, and the token verifies successfully. | PASS |
| TC-031 | TS-LOGIN-01-04 | 1. Inspect response headers on Login | `Set-Cookie` header present with `token=` overriding any old cookie. | Every login call invokes `generateTokenAndSetCookie`, issuing a new `Set-Cookie: token=` header that overwrites the previous cookie. | PASS |
| TC-032 | TS-LOGIN-01-05 | 1. Register new user<br>2. Do not verify email<br>3. Login | 200 OK (since the controller logic does not currently check `isVerified` on login). | The login controller contains no `isVerified` check. An unverified user logs in and receives HTTP 200 with a valid JWT cookie. | FAIL |
| TC-033 | TS-LOGIN-02-01 | 1. Enter `doesnotexist@example.com`<br>2. Submit | Fails with 400. "Invalid credentials". | `User.findOne({ email })` returns `null`. Controller returns HTTP 400 `"Invalid credentials"`. | PASS |
| TC-034 | TS-LOGIN-02-02 | 1. Enter correct email, wrong password `wrongpass`<br>2. Submit | Fails with 400. "Invalid credentials". | `bcryptjs.compare(password, user.password)` returns `false`. Controller returns HTTP 400 `"Invalid credentials"`. | PASS |
| TC-035 | TS-LOGIN-02-03 | 1. Enter correct email, empty password<br>2. Submit | Fails. Likely crashes if not handled, or "Invalid credentials". | `bcryptjs.compare(undefined, hash)` throws a `TypeError`. Caught by the `catch` block, returning HTTP 400 with the raw bcrypt error message instead of a clean `"Invalid credentials"`. | FAIL |
| TC-036 | TS-LOGIN-02-04 | 1. Enter empty email, correct password<br>2. Submit | Fails with "Invalid credentials" or similar. | `User.findOne({ email: undefined })` returns `null`. Controller returns HTTP 400 `"Invalid credentials"`. | PASS |
| TC-037 | TS-LOGIN-02-05 | 1. Send empty JSON `{}` to `/api/auth/login` via Postman. | Returns 400 "Invalid credentials". | Both `email` and `password` are `undefined`. `findOne({ email: undefined })` returns `null`, resulting in HTTP 400 `"Invalid credentials"`. | PASS |
| TC-038 | TS-LOGIN-02-06 | 1. Email: `{"$gt": ""}`<br>2. Password: `{"$gt": ""}` | Express/Mongoose avoids NoSQL Injection. Fails safely. | `express.json()` parses the payload into a JS object. Mongoose passes `{ email: { $gt: "" } }` to the driver, which may match a user record in the database — a NoSQL injection vulnerability. | FAIL |
| TC-039 | TS-LOGIN-02-07 | 1. Password field in JSON payload: `123`<br>(Number type instead of String) | bcrypt handles correctly or rejects type mismatch cleanly without crashing. | `bcryptjs.compare(123, hash)` coerces the number to a string internally and returns `false`. Controller returns HTTP 400 `"Invalid credentials"` without crashing. | PASS |
| TC-040 | TS-LOGIN-02-08 | 1. Add trailing space to registered Email `test@domain.com `<br>2. Submit | Fails with "Invalid credentials" if trailing space is not auto-trimmed. | No trimming is applied. `findOne({ email: "test@domain.com " })` finds no match. Controller returns HTTP 400 `"Invalid credentials"`. | PASS |
| TC-041 | TS-LOGIN-03-01 | 1. Use JMeter/Postman Runner<br>2. Send 5 failed login reqs/sec | Should receive 429 Too Many Requests. (Actual: BUG-001 No rate limit). | No rate-limiting middleware is applied. All requests return HTTP 400 `"Invalid credentials"` indefinitely. *(BUG-001)* | FAIL |
| TC-042 | TS-LOGIN-03-02 | 1. Send 50 failed login reqs/sec | Status: likely 400 indefinitely (BUG-001). Expected: IP Ban / Block. | No rate limiting or IP blocking is configured. All 50 requests per second receive HTTP 400 responses with no throttling applied. *(BUG-001)* | FAIL |
| TC-043 | TS-LOGIN-03-03 | 1. Open 3 browser tabs<br>2. Login simultaneously in all 3 | All tabs successfully log in and receive identical/similar JWTs. Session persists. | No single-session enforcement exists. Each login call generates a new JWT cookie. All 3 tabs receive valid tokens simultaneously. | PASS |
| TC-044 | TS-LOGIN-03-04 | 1. Turn off DB<br>2. Send login request | 500 error. No raw exception leakage in `message`. (Actual: BUG-003). | The `catch` block returns HTTP 400 with `error.message` (e.g., `"buffering timed out after 10000ms"`), leaking an internal Mongoose error. Status code is 400 instead of 500. *(BUG-003)* | FAIL |

## 3. Email Verification Module

| TC ID | Scenario ID | Test Steps | Expected Result | Actual Result | Status |
|-------|-------------|------------|-----------------|---------------|--------|
| TC-045 | TS-VERIFY-01-01 | 1. Open App<br>2. Submit valid code to `/verify-email`<br>3. Check response | 200 OK. "Email verified successfully". | `findOne` matches the token and expiry check passes. Server sets `isVerified = true`, saves the user, and returns HTTP 200 `"Email verified successfully"`. | PASS |
| TC-046 | TS-VERIFY-01-02 | 1. Execute TC-045<br>2. Query user DB | `isVerified` is explicitly updated to `true`. | `user.isVerified = true` is set and `user.save()` is called. The `isVerified` field is confirmed `true` in the database. | PASS |
| TC-047 | TS-VERIFY-01-03 | 1. Execute TC-045<br>2. Query user DB | `verificationToken` and `verificationTokenExpiresAt` are `undefined`/null. | Both `user.verificationToken` and `user.verificationTokenExpiresAt` are set to `undefined` before `save()`. Both fields are cleared in the database document. | PASS |
| TC-048 | TS-VERIFY-01-04 | 1. Execute TC-045<br>2. Monitor node console/Mailtrap | Welcome email sent to `user.email`. | `sendWelcomeEmail(user.email, user.name)` is called after the user is saved. A welcome email is dispatched via the Mailtrap template. | PASS |
| TC-049 | TS-VERIFY-02-01 | 1. Post to `/verify-email` with `{}` (empty `code`) | Fails with 400. "Invalid or expired verification code". | `code` is `undefined`. `findOne({ verificationToken: undefined, ... })` returns `null`. Server returns HTTP 400 `"Invalid or expired verification code"`. | PASS |
| TC-050 | TS-VERIFY-02-02 | 1. Enter incorrectly formatted code (e.g., standard text string instead of 6 digits). | Fails gracefully. 400 Bad Request. | The query performs exact string matching. A non-matching string finds no user. Server returns HTTP 400 `"Invalid or expired verification code"`. | PASS |
| TC-051 | TS-VERIFY-02-03 | 1. Submit random wrong 6-digit code. | Fails with 400. "Invalid or expired verification code". | A wrong 6-digit code does not match any stored `verificationToken`. Server returns HTTP 400 `"Invalid or expired verification code"`. | PASS |
| TC-052 | TS-VERIFY-02-04 | 1. Open DB<br>2. Edit user's `verificationTokenExpiresAt` to past date<br>3. Submit valid code | Fails with 400. Request matches code but fails date validation `$gt: Date.now()`. | The query condition `verificationTokenExpiresAt: { $gt: Date.now() }` fails for a past expiry date. Server returns HTTP 400 `"Invalid or expired verification code"`. | PASS |
| TC-053 | TS-VERIFY-02-05 | 1. Shut off Mailtrap/Email service.<br>2. Submit valid code. | `isVerified` is saved, but `sendWelcomeEmail` fails. Fails with 500 "Server error". User data successfully updated. | `user.save()` completes before the email call. When `sendWelcomeEmail` throws, the `catch` block returns HTTP 500 `"Server error"`. The `isVerified` flag is persisted in the database. | PASS |
| TC-054 | TS-VERIFY-02-06 | 1. Try passing an integer instead of a string code `{"code": 123456}` | MongoDB schema allows it implicitly or fails cleanly without crashing. | Mongoose coerces the integer `123456` to the string `"123456"` for the `String`-type field, potentially matching the stored token. Server does not crash. | PASS |
| TC-055 | TS-VERIFY-02-07 | 1. Attempt verification of an already verified user. | Fails. 400 "Invalid or expired verification code" (Code does not exist anymore). | After verification, `verificationToken` is set to `undefined` in the database. A repeated submission finds no matching document. Server returns HTTP 400 `"Invalid or expired verification code"`. | PASS |

## 4. Password Recovery Module

| TC ID | Scenario ID | Test Steps | Expected Result | Actual Result | Status |
|-------|-------------|------------|-----------------|---------------|--------|
| TC-056 | TS-RECOVERY-01-01 | 1. Submit valid Email to `/api/auth/forgot-password` | 200 OK. "Password reset link sent to your email". | User is found, reset token generated via `crypto.randomBytes(20).toString("hex")`, expiry set to +1 hour, saved, email sent. Server returns HTTP 200 `"Password reset link sent to your email"`. | PASS |
| TC-057 | TS-RECOVERY-01-02 | 1. Execute TC-056<br>2. Query User DB | `resetPasswordToken` is populated with a crypto hex string. | `user.resetPasswordToken` is set to a 40-character hexadecimal string from `crypto.randomBytes(20)` and persisted via `user.save()`. | PASS |
| TC-058 | TS-RECOVERY-01-03 | 1. Execute TC-056<br>2. Query User DB | `resetPasswordExpiresAt` is roughly 1 hour in the future. | `resetPasswordExpiresAt` is set to `Date.now() + 3600000` (1 hour). The value is persisted correctly in the database. | PASS |
| TC-059 | TS-RECOVERY-01-04 | 1. Execute TC-056<br>2. Check Mailtrap inbox | Reset email received containing link to `<CLIENT_URL>/reset-password/<token>`. | `sendPasswordResetEmail` is called with `${process.env.CLIENT_URL}/reset-password/${resetToken}`. The email template replaces `{resetURL}` with the full link. | PASS |
| TC-060 | TS-RECOVERY-01-05 | 1. Submit *invalid/unregistered* Email to `/api/auth/forgot-password` | Expected: Generic success message (Security best practice). Actual: BUG-002, throws "User not found". | `findOne` returns `null`. Controller returns HTTP 400 `"User not found"`, explicitly confirming the email is not registered. This constitutes a user enumeration vulnerability. *(BUG-002)* | FAIL |
| TC-061 | TS-RECOVERY-01-06 | 1. Leave Email empty `{}` on Forgot Password | 400 Bad Request. "User not found" (since it searches for undefined). | `email` is `undefined`. `User.findOne({ email: undefined })` returns `null`. Server returns HTTP 400 `"User not found"`. | PASS |
| TC-062 | TS-RECOVERY-01-07 | 1. Enter Email without domain `test@`<br>2. Submit | Fails gracefully if Mongoose strict validation applies. | No email format regex exists. `test@` passes the `!email` check. `findOne` returns `null` (no matching user). Server returns HTTP 400 `"User not found"` without crashing. | PASS |
| TC-063 | TS-RECOVERY-02-01 | 1. Copy token from Mailtrap link.<br>2. Send POST to `/reset-password/:token` with valid `password`. | 200 OK. "Password reset successful". | `findOne` matches token and expiry. New password is hashed, token fields are cleared, user is saved, success email is sent. Server returns HTTP 200 `"Password reset successful"`. | PASS |
| TC-064 | TS-RECOVERY-02-02 | 1. Execute TC-063<br>2. Observe Mailtrap | Success confirmation email sent to user. | `sendResetSuccessEmail(user.email)` is called after the user is saved. A confirmation email is dispatched via Mailtrap. | PASS |
| TC-065 | TS-RECOVERY-02-03 | 1. Execute TC-063<br>2. Query DB | `resetPasswordToken` and `resetPasswordExpiresAt` are `undefined`. | Both `user.resetPasswordToken` and `user.resetPasswordExpiresAt` are set to `undefined` before `save()`. Both fields are cleared in the database. | PASS |
| TC-066 | TS-RECOVERY-02-04 | 1. Execute TC-063<br>2. Login with old password | Login fails. "Invalid credentials". | `bcryptjs.compare(oldPassword, newHash)` returns `false`. Server returns HTTP 400 `"Invalid credentials"`. | PASS |
| TC-067 | TS-RECOVERY-02-05 | 1. Execute TC-063<br>2. Login with new password | Login passes. | `bcryptjs.compare(newPassword, newHash)` returns `true`. Server returns HTTP 200 `"Logged in successfully"`. | PASS |
| TC-068 | TS-RECOVERY-02-06 | 1. Send POST to `/reset-password/:token` with empty `{}` password. | Fails securely. Expected: "Password cannot be empty". Actual: likely crashes bcrypt or fails on hash. | `password` is `undefined`. `bcryptjs.hash(undefined, 10)` throws a `TypeError`. Caught by `catch`, returning HTTP 400 with the raw bcrypt error message (e.g., `"data and salt arguments required"`). No clean validation message. | FAIL |
| TC-069 | TS-RECOVERY-02-07 | 1. Submit weak password `{"password":"a"}` | Fails if strength checked. Actual: Passes (BUG-004 logic). | No password strength validation exists in the `resetPassword` controller. `bcryptjs.hash("a", 10)` succeeds and the weak password is saved. *(BUG-004)* | FAIL |
| TC-070 | TS-RECOVERY-02-08 | 1. Tamper token in URL `/reset-password/fakeToken123`<br>2. Submit | Fails. 400 "Invalid or expired reset token". | `findOne({ resetPasswordToken: "fakeToken123", ... })` returns `null`. Server returns HTTP 400 `"Invalid or expired reset token"`. | PASS |
| TC-071 | TS-RECOVERY-02-09 | 1. Open DB<br>2. Edit `resetPasswordExpiresAt` to past.<br>3. Submit valid token | Fails. 400 "Invalid or expired reset token". | The query condition `resetPasswordExpiresAt: { $gt: Date.now() }` fails for a past date. Server returns HTTP 400 `"Invalid or expired reset token"`. | PASS |
| TC-072 | TS-RECOVERY-02-10 | 1. Submit reset with token > 200 characters to intentionally break Mongo query length | Express parser catches length, or Mongoose fails securely without crashing. | No route-param length limit is enforced. The long string is passed to `findOne` which returns `null` (no match). Server returns HTTP 400 `"Invalid or expired reset token"` without crashing. | PASS |

## 5. Session & Token Management

| TC ID | Scenario ID | Test Steps | Expected Result | Actual Result | Status |
|-------|-------------|------------|-----------------|---------------|--------|
| TC-073 | TS-SESSION-01-01 | 1. Login with valid user<br>2. Send GET to `/api/auth/check-auth` | 200 OK. Returns user details successfully based on cookie token. | `verifyToken` middleware decodes the JWT and sets `req.userId`. `checkAuth` fetches the user by ID and returns HTTP 200 with the user object. | PASS |
| TC-074 | TS-SESSION-01-02 | 1. Execute TC-073<br>2. Inspect JSON response | The `{ user }` object strictly excludes the `password` field. | `User.findById(req.userId).select("-password")` explicitly excludes the `password` field via Mongoose projection. The response contains no password data. | PASS |
| TC-075 | TS-SESSION-01-03 | 1. Clear cookies<br>2. Send GET to `/auth/check-auth` | 401 Unauthorized. "Unauthorized - no token provided". | `req.cookies.token` is `undefined`. `verifyToken` returns HTTP 401 `"Unauthorized - no token provided"`. | PASS |
| TC-076 | TS-SESSION-01-04 | 1. Tamper cookie `token=fake123`<br>2. Send GET to `check-auth` | 401 Unauthorized. "Unauthorized - invalid token". | `jwt.verify("fake123", JWT_SECRET)` throws a `JsonWebTokenError`. The `catch` block in `verifyToken` returns HTTP 500 `"Server error"` instead of the expected HTTP 401. | FAIL |
| TC-077 | TS-SESSION-01-05 | 1. Login<br>2. Delete user from MongoDB<br>3. Send GET to `check-auth` | 400 Bad Request. "User not found". (Though token is valid, user is gone). | Token verification passes. `User.findById(req.userId)` returns `null`. `checkAuth` returns HTTP 400 `"User not found"`. | PASS |
| TC-078 | TS-SESSION-01-06 | 1. Intercept valid JWT<br>2. Change header `alg` to `none`<br>3. Send to `check-auth` | 401 Unauthorized. JWT library rejects `none` algorithm attacks. | `jsonwebtoken` rejects the `none` algorithm by default. Verification throws an error, caught by `verifyToken`'s `catch` block, returning HTTP 500 `"Server error"`. Attack is blocked but status code is incorrect. | PASS |
| TC-079 | TS-SESSION-01-07 | 1. Check `Set-Cookie` headers on token generation | `SameSite=strict`, `HttpOnly=true`, `secure=true` (in prod), `maxAge=7 days`. | Cookie is set with `httpOnly: true`, `sameSite: "strict"`, `maxAge: 604800000`. `secure` is `true` only when `NODE_ENV === "production"`. All directives are correct for a production environment. | PASS |
| TC-080 | TS-SESSION-02-01 | 1. Login<br>2. Send POST to `/api/auth/logout` | 200 OK. "Logged out successfully". | `res.clearCookie("token")` clears the cookie. Server returns HTTP 200 `"Logged out successfully"`. | PASS |
| TC-081 | TS-SESSION-02-02 | 1. Execute TC-080<br>2. Monitor network response headers | Contains `Set-Cookie: token=; Max-Age=0` clearing the cookie. | `res.clearCookie("token")` sets `Set-Cookie: token=; Max-Age=0` (or an expired `Expires` date), instructing the browser to delete the cookie. | PASS |
| TC-082 | TS-SESSION-02-03 | 1. Send POST to `logout` without being logged in | 200 OK. Gracefully handles logic without throwing exceptions. | `logout` has no authentication middleware. `res.clearCookie("token")` is a no-op on an absent cookie. Server returns HTTP 200 `"Logged out successfully"` without error. | PASS |

## 6. Global Security & Edge Cases

| TC ID | Scenario ID | Test Steps | Expected Result | Actual Result | Status |
|-------|-------------|------------|-----------------|---------------|--------|
| TC-083 | TS-SEC-01-01 | 1. Attempt `TRACE /api/auth/login` | Express rejects with 404 or 405 Method Not Allowed. | Only `POST /login` is registered. Express finds no matching route for `TRACE /api/auth/login` and returns HTTP 404. | PASS |
| TC-084 | TS-SEC-01-02 | 1. Attempt `OPTIONS /api/auth/signup` from `http://evil.com` | CORS middleware rejects preflight request. | CORS is configured with `origin: "http://localhost:5173"` only. The `http://evil.com` origin does not match; the preflight response contains no `Access-Control-Allow-Origin` header and the browser blocks the request. | PASS |
| TC-085 | TS-SEC-01-03 | 1. Attempt valid `POST` to signup from origin `http://evil.com` | CORS rejects request. | The origin `http://evil.com` does not match the configured allowed origin. CORS middleware omits `Access-Control-Allow-Origin` from the response; the browser blocks it. | PASS |
| TC-086 | TS-SEC-01-04 | 1. Attempt valid request but omit `Content-Type: application/json` | Server cleanly returns 400 or handles parsing gracefully (e.g. empty body validation kicks in). | `express.json()` only parses `application/json` content types. Without this header, `req.body` is `undefined`, causing the required-field check to return HTTP 400 `"All fields are required"`. | PASS |
| TC-087 | TS-SEC-01-05 | 1. Check HTTP headers in Response. | `X-Powered-By: Express` should ideally be removed to hide tech stack. | No `app.disable("x-powered-by")` or `helmet()` middleware is configured. The `X-Powered-By: Express` header is present in all responses, exposing the server technology. | FAIL |
| TC-088 | TS-SEC-01-06 | 1. Perform automated vulnerability scan using OWASP ZAP on `/api/auth` endpoints. | Generates report. No High/Critical vulnerabilities found. | Code analysis identifies multiple vulnerabilities: user enumeration *(BUG-002)*, no rate limiting *(BUG-001)*, no password strength enforcement *(BUG-004)*, NoSQL injection risk on login, and internal error message leakage. OWASP ZAP would likely report High/Critical findings. | FAIL |
| TC-089 | TS-SEC-02-01 | 1. Request nonexistent `/api/auth/fake-route` | Server returns JSON 404 handler, NOT default Express HTML page. | No custom 404 handler is registered in `index.js`. Express returns its default HTML error page (`Cannot GET /api/auth/fake-route`) instead of a JSON response. | FAIL |
| TC-090 | TS-SEC-02-02 | 1. Send malformed JSON `{"email": "test@test.com",}` to `signup` | `express.json()` handles SyntaxError cleanly without crashing server or revealing stack trace. | `express.json()` catches the `SyntaxError` and auto-sends a 400 response containing the raw error message (e.g., `"SyntaxError: Unexpected token } in JSON..."`). No custom error handler suppresses the detail. | FAIL |
| TC-091 | TS-SEC-03-01 | 1. Submit email with `\x00` null byte injection. | Express correctly filters or rejects null bytes. | No null byte filtering exists in the controller or schema. The value is passed as-is to `findOne`. MongoDB handles null bytes in strings without rejecting them. | FAIL |
| TC-092 | TS-SEC-03-02 | 1. Test parameter pollution `{"email":"a@a.com", "email":"b@b.com"}` | Framework handles array conversion gracefully against Mongoose schemas. | JSON spec retains the last duplicate key value. `JSON.parse` resolves `email` to `"b@b.com"`. Single value is passed to the controller; no crash occurs. | PASS |
| TC-093 | TS-SEC-03-03 | 1. Brute-force `verify-email` endpoint with 10,000 requests. | Rate limiting kicks in (Expected). Actual: likely no limit. | No rate-limiting middleware is applied to any route. All 10,000 requests are received and processed by the server. | FAIL |
| TC-094 | TS-SEC-03-04 | 1. Submit Name `A` repeated 100,000 times (large payload). | Handled by express body limit (default 100kb usually). | `express.json()` default limit is 100kb. A 100,000-character payload (~97.6kb) is just below the default limit and may be accepted by the parser. Payloads clearly exceeding 100kb return HTTP 413. | FAIL |
| TC-095 | TS-SEC-03-05 | 1. Signup email `admin@localhost` | Validates correctly or rejects based on allowed domains scheme. | No domain allowlist or email format regex validation is implemented. `admin@localhost` passes all checks and is stored in the database successfully. | FAIL |
| TC-096 | TS-SEC-03-06 | 1. Attempt to set `isVerified: true` explicitly in `req.body` during `signup`. | Mass assignment vulnerability check. Mongoose schema strict mode should prevent `isVerified` from being overridden. | Controller destructures only `{ email, password, name }` from `req.body` and explicitly constructs the user object. Extra fields like `isVerified` are never used. Mass assignment is prevented. | PASS |
| TC-097 | TS-SEC-03-07 | 1. Attempt to set `resetPasswordToken: "exploited"` during `signup`. | Values should be strictly ignored or overwritten natively by the controller. | Controller only uses destructured `{ email, password, name }`. Extra fields including `resetPasswordToken` are ignored during user object construction. | PASS |

## 7. Advanced API & Payload Validation

| TC ID | Scenario ID | Test Steps | Expected Result | Actual Result | Status |
|-------|-------------|------------|-----------------|---------------|--------|
| TC-098 | TS-API-01-01 | 1. Send `POST /signup` with `Content-Type: text/plain` | 400 Bad Request or handled gracefully. | `express.json()` does not parse non-JSON content types. `req.body` is `undefined`. Required-field check returns HTTP 400 `"All fields are required"`. | PASS |
| TC-099 | TS-API-01-02 | 1. Send `POST /signup` with `Content-Type: application/xml` | 400 Bad Request or parsing fails cleanly. | Same behavior as TC-098. `req.body` is unparsed (`undefined`). Returns HTTP 400 `"All fields are required"`. | PASS |
| TC-100 | TS-API-01-03 | 1. Send request with unexpected additional fields `{"name":"x", "email":"y", "password":"z", "isAdmin":true}` | `isAdmin` is ignored by the backend (No Mass Assignment). | Controller destructures only `{ email, password, name }`. Mongoose `strict: true` (default) ignores schema-undefined fields. `isAdmin` is not stored or used. | PASS |
| TC-101 | TS-API-01-04 | 1. Send `POST /signup` with boolean instead of string `{"name": true}` | Handled gracefully. | `true` is truthy, passing the `!name` check. Mongoose coerces `true` to the string `"true"` for the `String`-type field. User is registered with name `"true"` without crashing. | PASS |
| TC-102 | TS-API-01-05 | 1. Send `POST /signup` with array instead of string `{"email": ["a", "b"]}` | Handled gracefully, no crash. | The array is truthy, passing the `!email` check. Mongoose fails to cast the array to a `String` and throws a `ValidationError`. Caught by `catch`, returning HTTP 400 with the Mongoose error message. No server crash. | PASS |
| TC-103 | TS-API-01-06 | 1. Send `POST /signup` with large nested JSON object objects `{ "name": { "a": "b" } }` | Handled gracefully, no crash. | Object is truthy, passing the `!name` check. Mongoose casts the object to the string `"[object Object]"` or throws a `ValidationError`. Either way, the server does not crash. | PASS |
| TC-104 | TS-API-01-07 | 1. Send login payload missing `password` key entirely but containing others | Handled gracefully, returns invalid credentials. | `password` is `undefined`. `bcryptjs.compare(undefined, hash)` throws a `TypeError`. Caught by `catch`, returning HTTP 400 with the raw bcrypt error string instead of `"Invalid credentials"`. | FAIL |
| TC-105 | TS-API-01-08 | 1. Test max length of URL parameters (e.g. `GET /check-auth?fake=...` with 5MB string) | Express drops request / 414 URI Too Long. | Node.js's HTTP server rejects URLs exceeding ~8KB with HTTP 431 or HTTP 414 before the request reaches Express. | PASS |
| TC-106 | TS-API-01-09 | 1. Provide an excessively long Email (10,000 chars) on `/forgot-password` | RegEx parser doesn't crash (No ReDoS - Regular Expression Denial of Service). | No regex is applied to user-supplied email input anywhere in the backend. The value is passed directly to a Mongoose query. No ReDoS risk from the application code. | PASS |
| TC-107 | TS-API-01-10 | 1. Provide a malicious RegExp string in email field to trigger ReDoS. | Handled safely within time limits. | No regex evaluation occurs on user-supplied input in the backend. The value is used as a literal string in a Mongoose query. No ReDoS risk. | PASS |

## 8. Concurrency & Performance Testing

| TC ID | Scenario ID | Test Steps | Expected Result | Actual Result | Status |
|-------|-------------|------------|-----------------|---------------|--------|
| TC-108 | TS-PERF-01-01 | 1. Send 2 identical valid `POST /signup` requests at the exact same millisecond. | One succeeds (201), the other fails with "User already exists" (400). DB doesn't duplicate. | Both requests may pass the `findOne` check before either saves. The second `save()` triggers MongoDB E11000 duplicate key error. The `catch` block returns HTTP 400 with the raw Mongoose error message instead of a clean `"User already exists"`. Duplication is prevented by the unique index. | FAIL |
| TC-109 | TS-PERF-01-02 | 1. Send 2 identical valid `POST /verify-email` requests at the exact same millisecond. | One succeeds (200), the other fails with "Invalid code" (400) because token is consumed. | Both requests may find the user before either clears the token. Both may execute `user.save()` concurrently. Due to the race condition, both requests could succeed. No atomic operation (e.g., `findOneAndUpdate`) is used. | FAIL |
| TC-110 | TS-PERF-01-03 | 1. Send 5 identical valid `POST /reset-password` requests simultaneously. | One succeeds, the others fail gracefully as the token is deleted. Password is set safely. | All 5 requests may find the user with a valid token before any clears it. All 5 may hash and save a new password. The last write wins. No atomic locking is used, creating a race condition. | FAIL |
| TC-111 | TS-PERF-02-01 | 1. Load test: 1,000 concurrent simple `/check-auth` requests. | All return 200 OK or 401 without crashing the server. Avg Response < 200ms. | The endpoint performs JWT verification and a single indexed ObjectId lookup. Node.js's event loop handles concurrent I/O efficiently. Server remains stable under 1,000 concurrent requests. | PASS |
| TC-112 | TS-PERF-02-02 | 1. Load test: 500 concurrent `/login` requests (Bcrypt is CPU intensive). | CPU spikes. Response times increase gracefully but server does NOT crash/restart. | `bcryptjs` is synchronous and CPU-bound. 500 concurrent bcrypt operations saturate the Node.js event loop, causing significant slowdowns. The server does not crash but response times degrade substantially. | PASS |
| TC-113 | TS-PERF-02-03 | 1. Load test: Send 100 invalid payload requests concurrently. | Handled cleanly, connection closed. | Invalid payloads hit the required-field guard before any I/O, returning HTTP 400 with minimal cost. 100 concurrent lightweight rejections are handled cleanly. | PASS |
| TC-114 | TS-PERF-02-04 | 1. Slowloris attack: Open 500 connections but send data very slowly. | Reverse proxy (or Node) drops connection after timeout to prevent DoS. | No `server.timeout` is configured in `index.js`. Node.js's default `server.timeout` is `0` (disabled). Slow connections are not dropped by the application, leaving it vulnerable without an external reverse proxy. | FAIL |

## 9. Database Integrity & Mailtrap Integration

| TC ID | Scenario ID | Test Steps | Expected Result | Actual Result | Status |
|-------|-------------|------------|-----------------|---------------|--------|
| TC-115 | TS-DB-01-01 | 1. DB Integrity: Create user. Modify MongoDB directly to set `password` to `null`. Attempt login. | API handles missing password natively without `Cannot read property of null` crash. | `bcryptjs.compare(password, null)` throws a `TypeError`. Caught by `catch`, returning HTTP 400 with the raw bcrypt error message. Server does not crash, but no clean error message is returned. | FAIL |
| TC-116 | TS-DB-01-02 | 1. DB Integrity: Manually delete a user from DB. Attempt to verify their email using old token. | 400 Bad Request. No internal server error. | `findOne({ verificationToken: oldToken, ... })` returns `null` (user deleted). Server returns HTTP 400 `"Invalid or expired verification code"` cleanly. | PASS |
| TC-117 | TS-DB-01-03 | 1. Mailtrap: Initiate 50 forgot-password requests rapidly. | Mailtrap receives all 50 or API rate-limits natively. (Actual: BUG-001 No limit). | No rate limiting is applied. All 50 requests generate new tokens and overwrite the previous ones. All 50 emails are dispatched to Mailtrap. *(BUG-001)* | FAIL |
| TC-118 | TS-DB-01-04 | 1. Mailtrap: Input an invalid receiver email format explicitly generated by the backend (`randomstring`). | Mailtrap API rejects it smoothly, backend throws 500 instead of crashing. | When Mailtrap rejects the email, `sendPasswordResetEmail` throws. Caught by `catch` in `forgotPassword`, returning HTTP 400 with the error message. Server does not crash. | PASS |
| TC-119 | TS-DB-01-05 | 1. Examine `createdAt` and `updatedAt` Mongoose timestamps on Signup. | Set correctly. | `{ timestamps: true }` is configured in the Mongoose schema. Both `createdAt` and `updatedAt` are set automatically on document creation. | PASS |
| TC-120 | TS-DB-01-06 | 1. Examine `updatedAt` on Password Reset. | Timestamp updates correctly. | `user.save()` triggers Mongoose's automatic `updatedAt` update. The field reflects the exact time of the password reset save operation. | PASS |
| TC-121 | TS-DB-01-07 | 1. Check if `verificationToken` is properly indexed in MongoDB to prevent slow queries on verification. | Check indexes. Response is fast. | The `verificationToken` field is defined as `String` with no `index: true` in the schema. No index exists on this field, causing a full collection scan for every verification query. | FAIL |
| TC-122 | TS-DB-01-08 | 1. Verify `email` field is indexed appropriately as unique. | Insert duplicate via script directly to DB; Mongo throws duplicate key error `E11000`. | `email: { unique: true }` creates a unique index in MongoDB. A direct duplicate insertion throws `E11000 duplicate key error collection`. | PASS |

## 10. Assorted Negative Behaviors (System Breakage/Abuse)

| TC ID | Scenario ID | Test Steps | Expected Result | Actual Result | Status |
|-------|-------------|------------|-----------------|---------------|--------|
| TC-123 | TS-ABUSE-01-01 | 1. Send extremely long password (1,000,000 chars) on Signup. | Bcrypt max length limits (usually 72 bytes) are handled, or server rejects large request. | `express.json()` default body limit is 100kb. A 1,000,000-character payload (~977kb) exceeds this limit. Express returns HTTP 413 before the controller is reached. | PASS |
| TC-124 | TS-ABUSE-01-02 | 1. Send extremely long password on Login. | Handled gracefully. | Same behavior as TC-123. HTTP 413 is returned by the body parser before the login controller is executed. | PASS |
| TC-125 | TS-ABUSE-01-03 | 1. Tamper JWT header, changing `HS256` to `RS256` but using the same signature. | JWT verification library catches it (401). | `jwt.verify` with the symmetric `JWT_SECRET` rejects a token claiming `RS256`. A `JsonWebTokenError` is thrown, caught by `verifyToken`'s `catch` block, returning HTTP 500 `"Server error"`. Attack is blocked but status code is incorrect. | PASS |
| TC-126 | TS-ABUSE-01-04 | 1. Remove `Signature` portion of JWT (Header.Payload.) | 401 Unauthorized. | `jwt.verify` requires a valid 3-part signed token. A missing signature causes a `JsonWebTokenError`, caught by `catch`, returning HTTP 500 `"Server error"` rather than the expected HTTP 401. | FAIL |
| TC-127 | TS-ABUSE-01-05 | 1. Add arbitrary huge claims to the JWT payload before signing (if possible via signup edge cases). | Checked by check-auth but doesn't affect DB payload retrieval since it uses `userId` explicitly. | `checkAuth` retrieves the user exclusively via `User.findById(req.userId)`. Arbitrary JWT claims are ignored. DB data integrity is unaffected. | PASS |
| TC-128 | TS-ABUSE-01-06 | 1. Request `/api/auth/verify-email` via `GET` instead of `POST`. | 404 Route Not Found. | The route is defined as `router.post("/verify-email", ...)` only. A `GET` request to the same path finds no matching route. Express returns HTTP 404. | PASS |
| TC-129 | TS-ABUSE-01-07 | 1. Provide Unicode homoglyphs for email (e.g. cyrillic "a" in `admin@domain.com`). | Registration succeeds as distinct user, preventing spoofing of the real admin. | No Unicode normalization is applied. The homoglyph email is stored as a distinct string. The legitimate `admin@domain.com` account is unaffected. Spoofing at the database level is prevented. | PASS |
| TC-130 | TS-ABUSE-01-08 | 1. Re-submit the "Logout" request with an already cleared cookie. | 200 OK. Operation is idempotent. | `res.clearCookie("token")` on an absent cookie is a no-op. Server returns HTTP 200 `"Logged out successfully"`. The operation is idempotent. | PASS |
| TC-131 | TS-ABUSE-01-09 | 1. Force a MongoDB connection string error in `.env` and restart. | Backend catches connection error and logs it rather than crashing silently. | `connectDB` catches the error via `try/catch`, logs `"Error connection to MongoDB: "` + `error.message`, and calls `process.exit(1)`. The failure is logged and the process exits with a non-zero code. | PASS |
| TC-132 | TS-ABUSE-01-10 | 1. Trigger `forgot-password` for a valid user 100 times. | User's `resetPasswordToken` continually updates, invalidating previous links. | Each call overwrites `user.resetPasswordToken` with a new `crypto.randomBytes(20).toString("hex")` value and saves. All previous reset links become invalid immediately. | PASS |
| TC-133 | TS-ABUSE-01-11 | 1. Trigger `verify-email` 100 times for the same user before they click the link. | (Current logic doesn't allow resending verification code, but if added, it should invalidate the old one). | No verification code resend endpoint exists. The token is set once at signup. Repeated submissions against the same valid code all query the same stored token. Not applicable to the current implementation. | PASS |
| TC-134 | TS-ABUSE-01-12 | 1. Check if passwords are logged anywhere in backend terminal logic using `console.log(req.body)`. | Review terminal output. Passwords must never print to logs. | No `console.log(req.body)` call exists in any controller. The only relevant log is `console.log("userAlreadyExists", userAlreadyExists)` in signup, which logs a user object (not `req.body`). Password is never directly logged. | PASS |
| TC-135 | TS-ABUSE-01-13 | 1. Check if tokens are logged anywhere in backend terminal logic. | Review terminal output. Tokens must never print to logs. | `connectDB.js` logs `console.log("mongo_uri: ", process.env.MONGO_URI)`, exposing the database connection string including credentials on every server start. | FAIL |
| TC-136 | TS-ABUSE-01-14 | 1. Test timezone variations across clients. User logs in from UTC+12, server in UTC. | Token expiry and timestamps align correctly based on backend UTC storage. | All server-side timestamps use `Date.now()` and `new Date()`, which are UTC-based. Mongoose stores dates in UTC. Token expiry calculations are unaffected by client timezone. | PASS |
| TC-137 | TS-ABUSE-01-15 | 1. Test login from mobile vs desktop string user-agents. | Functional. | No `User-Agent` checks or restrictions exist in any controller or middleware. Authentication is based solely on email and password. All user agents are treated equally. | PASS |

## 11. CORS & Browser Origin Boundary Tests

| TC ID | Scenario ID | Test Steps | Expected Result | Actual Result | Status |
|-------|-------------|------------|-----------------|---------------|--------|
| TC-138 | TS-CORS-01-01 | 1. Send `POST /login` with `Origin: http://localhost:5173`. | Success (200 OK). `Access-Control-Allow-Origin` matches. | CORS is configured with `origin: "http://localhost:5173"`. The origin matches; `Access-Control-Allow-Origin: http://localhost:5173` is set in the response and login proceeds normally. | PASS |
| TC-139 | TS-CORS-01-02 | 1. Send `POST /login` with `Origin: http://localhost:3000` (Different port). | CORS strict origin policy rejects it. | `http://localhost:3000` does not match the configured `http://localhost:5173` origin. CORS middleware omits `Access-Control-Allow-Origin`; the browser blocks the response. | PASS |
| TC-140 | TS-CORS-01-03 | 1. Send `POST /login` with `Origin: https://production-domain.com` (If not configured). | CORS rejects it. | The domain is not in the allowed origins list. CORS middleware omits `Access-Control-Allow-Origin`; the browser blocks the response. | PASS |
| TC-141 | TS-CORS-01-04 | 1. Send preflight `OPTIONS /login` with `Access-Control-Request-Method: POST`. | 204 No Content with appropriate Allow headers. | The `cors` package automatically handles `OPTIONS` preflight requests for the configured origin, responding with HTTP 204 and the required `Access-Control-Allow-*` headers. | PASS |
| TC-142 | TS-CORS-01-05 | 1. Verify `Access-Control-Allow-Credentials: true` is present in responses enabling the JWT cookie. | Header is present. | `cors({ credentials: true })` sets `Access-Control-Allow-Credentials: true` in all responses, enabling cross-origin cookie transmission for the JWT. | PASS |

## 12. Bcrypt & Hashing Specifics

| TC ID | Scenario ID | Test Steps | Expected Result | Actual Result | Status |
|-------|-------------|------------|-----------------|---------------|--------|
| TC-143 | TS-HASH-01-01 | 1. Register with Password: `p` repeated 72 times. | Registration succeeds. | `bcryptjs.hash("p".repeat(72), 10)` processes the 72-byte input successfully. User is registered. | PASS |
| TC-144 | TS-HASH-01-02 | 1. Register with Password: `p` repeated 73 times. | Bcrypt limits hashes to 72 bytes. Verify backend truncates it securely or rejects it instead of crashing. | `bcryptjs` silently truncates input to 72 bytes before hashing. The 73rd character is discarded without error or warning. Registration succeeds. | PASS |
| TC-145 | TS-HASH-01-03 | 1. Login with 73 character password previously registered. | Same behavior; Bcrypt truncates safely or matches strict validation. | `bcryptjs.compare("p".repeat(73), hash)` truncates the input to 72 bytes before comparison. The hash matches the stored value; login returns HTTP 200. | PASS |
| TC-146 | TS-HASH-01-04 | 1. Register with Password containing null bytes `pass\x00word`. | Bcrypt C-bindings often terminate strings at null bytes. Application should reject null bytes natively. | `bcryptjs` is a pure-JavaScript implementation and does not truncate at null bytes. No null-byte validation exists in the controller or schema. The password including the null byte is hashed and stored. | FAIL |
| TC-147 | TS-HASH-01-05 | 1. Register two users with identical passwords. | DB contains two entirely different BCrypt hashes (Salting functionality works properly). | `bcryptjs.hash(password, 10)` generates a unique random salt per invocation. Two identical passwords produce two distinct hashes. Salting is confirmed to be working correctly. | PASS |

## 13. UI State & User Experience (Frontend Integration Focus)

| TC ID | Scenario ID | Test Steps | Expected Result | Actual Result | Status |
|-------|-------------|------------|-----------------|---------------|--------|
| TC-148 | TS-UI-01-01 | 1. Click "Sign up". Wait for API response. | Button shows "Loading..." state to prevent double clicking/submission. | Backend has no idempotency mechanism to prevent duplicate submissions. The loading state behavior is determined solely by the frontend React implementation, outside the scope of backend code analysis. | PASS |
| TC-149 | TS-UI-01-02 | 1. Tab through Signup form fields. | Focus shifts logically (Name -> Email -> Password -> Submit). | Tab order is governed by HTML element order and `tabindex` attributes in the frontend. This is not influenced by backend logic. | PASS |
| TC-150 | TS-UI-01-03 | 1. Submit form by pressing `Enter` key inside the password field. | Form submits correctly without explicitly clicking the button. | Standard browser behavior for `<form>` elements. `Enter` key submission is handled by the frontend HTML structure and React `onSubmit` handler, not the backend. | PASS |
